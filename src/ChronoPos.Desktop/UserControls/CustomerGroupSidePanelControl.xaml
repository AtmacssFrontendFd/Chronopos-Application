<UserControl x:Class="ChronoPos.Desktop.UserControls.CustomerGroupSidePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600"
             Background="{DynamicResource CardBackground}"
             FlowDirection="{Binding CurrentFlowDirection}"
             FontFamily="{DynamicResource PoppinsFontFallback}">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>

        <!-- Input Field Style -->
        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SurfaceBackground}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderLight}"
                Padding="30,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="{Binding FormTitle}" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource TextPrimary}"
                           VerticalAlignment="Center"/>
                
                <Button Grid.Column="1"
                        Content="✕"
                        Command="{Binding CancelCommand}"
                        Background="Transparent"
                        Foreground="{DynamicResource TextSecondary}"
                        BorderThickness="0"
                        FontSize="20"
                        Width="30"
                        Height="30"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="1"
                    SelectedIndex="{Binding SelectedTabIndex}"
                    Background="Transparent"
                    BorderThickness="0"
                    Margin="0">
            
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" 
                                        Background="Transparent"
                                        BorderThickness="0,0,0,2"
                                        BorderBrush="Transparent"
                                        Padding="20,12">
                                    <ContentPresenter ContentSource="Header" 
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Primary}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource Primary}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: Group Details -->
            <TabItem Header="Group Details">
        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="30,20">

                <!-- Validation Error Message -->
                <Border Background="#FFEBEE" 
                        BorderBrush="#F44336" 
                        BorderThickness="1" 
                        CornerRadius="4" 
                        Padding="15,10"
                        Margin="0,0,0,20"
                        Visibility="{Binding HasValidationErrors, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ValidationMessage}" 
                               Foreground="#C62828" 
                               FontSize="14"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Basic Information Section -->
                <TextBlock Text="Basic Information" 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           Margin="0,0,0,15"/>

                <!-- Group Name (English) -->
                <TextBlock Text="Group Name (English) *" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding EditingCustomerGroup.Name, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputFieldStyle}"
                         Margin="0,0,0,15"/>

                <!-- Group Name (Arabic) -->
                <TextBlock Text="Group Name (Arabic)" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding EditingCustomerGroup.NameAr, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputFieldStyle}"
                         FlowDirection="RightToLeft"
                         Margin="0,0,0,15"/>

                <!-- Status -->
                <CheckBox Content="Active Status" 
                          IsChecked="{Binding IsActive}"
                          Foreground="{DynamicResource TextPrimary}"
                          FontSize="14"
                          Margin="0,0,0,20"/>

                <!-- Pricing Section -->
                <TextBlock Text="Pricing &amp; Discounts" 
                           FontSize="18" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           Margin="0,10,0,15"/>

                <!-- Selling Price Type -->
                <TextBlock Text="Selling Price Type" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding SellingPriceTypes}"
                          SelectedItem="{Binding SelectedSellingPriceType}"
                          DisplayMemberPath="TypeName"
                          Style="{StaticResource ComboBoxStyle}"
                          Margin="0,0,0,15"/>

                <!-- Discount -->
                <TextBlock Text="Discount" Style="{StaticResource LabelStyle}"/>
                <ComboBox ItemsSource="{Binding Discounts}"
                          SelectedItem="{Binding SelectedDiscount}"
                          DisplayMemberPath="DiscountName"
                          Style="{StaticResource ComboBoxStyle}"
                          Margin="0,0,0,15"/>

                <!-- Discount Type -->
                <CheckBox Content="Percentage Discount" 
                          IsChecked="{Binding IsPercentageDiscount}"
                          Foreground="{DynamicResource TextPrimary}"
                          FontSize="14"
                          Margin="0,0,0,15"/>

                <!-- Discount Value -->
                <TextBlock Text="Discount Value" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding EditingCustomerGroup.DiscountValue, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputFieldStyle}"
                         Margin="0,0,0,15"/>

                <!-- Discount Max Value -->
                <TextBlock Text="Maximum Discount Value" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding EditingCustomerGroup.DiscountMaxValue, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputFieldStyle}"
                         Margin="0,0,0,30"/>

            </StackPanel>
        </ScrollViewer>
            </TabItem>

            <!-- Tab 2: Customers -->
            <TabItem Header="Customers">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="30,20">

                        <!-- Info Message -->
                        <Border Background="{DynamicResource SurfaceBackground}" 
                                BorderBrush="{DynamicResource BorderLight}" 
                                BorderThickness="1" 
                                CornerRadius="4" 
                                Padding="15,10"
                                Margin="0,0,0,20">
                            <TextBlock Text="Select customers to assign to this group. Customers can be added/removed at any time." 
                                       Foreground="{DynamicResource TextSecondary}" 
                                       FontSize="13"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <!-- Customer Search/Filter -->
                        <TextBlock Text="Search Customer" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding CustomerSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputFieldStyle}"
                                 Margin="0,0,0,15"/>

                        <!-- Available Customers List -->
                        <TextBlock Text="Available Customers" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   Margin="0,10,0,10"/>

                        <Border BorderBrush="{DynamicResource BorderLight}"
                                BorderThickness="1"
                                CornerRadius="4"
                                MaxHeight="300"
                                Margin="0,0,0,20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding FilteredCustomers}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="{DynamicResource BorderLight}"
                                                    BorderThickness="0,0,0,1"
                                                    Padding="10"
                                                    Background="Transparent">
                                                <Border.InputBindings>
                                                    <MouseBinding Gesture="LeftClick" 
                                                                Command="{Binding DataContext.ToggleCustomerSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"/>
                                                </Border.InputBindings>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0"
                                                            IsChecked="{Binding IsSelected}"
                                                            VerticalAlignment="Center"
                                                            Margin="0,0,10,0"
                                                            IsHitTestVisible="False"/>

                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding DisplayName}" 
                                                                 FontSize="14"
                                                                 FontWeight="SemiBold"
                                                                 Foreground="{DynamicResource TextPrimary}"/>
                                                        <TextBlock Text="{Binding MobileNo}" 
                                                                 FontSize="12"
                                                                 Foreground="{DynamicResource TextSecondary}"
                                                                 Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>

                        <!-- Selected Count -->
                        <TextBlock Margin="0,0,0,20">
                            <Run Text="{Binding SelectedCustomersCount, Mode=OneWay}" 
                                 FontWeight="SemiBold"
                                 Foreground="{DynamicResource Primary}"/>
                            <Run Text="customer(s) selected" 
                                 Foreground="{DynamicResource TextSecondary}"/>
                        </TextBlock>

                        <!-- Quick Actions -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Button Content="Select All" 
                                    Command="{Binding SelectAllCustomersCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="12,8"
                                    FontSize="12"
                                    Margin="0,0,10,0"/>
                            <Button Content="Clear All" 
                                    Command="{Binding ClearAllCustomersCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="12,8"
                                    FontSize="12"/>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Action Buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBackground}"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource BorderLight}"
                Padding="30,20">
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="{Binding SaveButtonText}"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>

                    <Button Grid.Column="2"
                            Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </Grid>
        </Border>
    </Grid>
</UserControl>
