<UserControl x:Class="ChronoPos.Desktop.Views.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             xmlns:controls="clr-namespace:ChronoPos.Desktop.Views.Controls"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             Background="{DynamicResource MainBackground}"
             FontFamily="{DynamicResource PoppinsFontFallback}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:CurrencyPriceConverter x:Key="CurrencyPriceConverter"/>
        <converters:LocalizationConverter x:Key="LocalizationConverter"/>
        
        <!-- Card Style - Theme Compatible -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Margin" Value="{DynamicResource DefaultMarginThickness}"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
        </Style>

        <!-- Summary Card Style - Theme Compatible -->
        <Style x:Key="SummaryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Padding" Value="20,16"/>
        </Style>

        <!-- Action Button Style - Theme Compatible -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource BorderCornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryAlt}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Primary}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BorderLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style - Theme Compatible -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="{DynamicResource BorderCornerRadius}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Action Button Style - Theme Compatible -->
        <Style x:Key="SmallActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input Style - Theme Compatible -->
        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>

        <!-- FormDatePickerStyle (from AddGoodsReturn) -->
        <Style x:Key="FormDatePickerStyle" TargetType="DatePicker">
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
        </Style>

        <!-- FormComboBoxStyle (from AddGoodsReturn) -->
        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{DynamicResource BorderCornerRadius}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ContentPresenter Grid.Column="0"
                                                    x:Name="PART_ContentPresenter"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                    Margin="{TemplateBinding Padding}"
                                                    IsHitTestVisible="False"/>
                                    
                                    <Border Grid.Column="1"
                                            Width="24"
                                            Height="24"
                                            Background="{DynamicResource CardBackground}"
                                            BorderBrush="{DynamicResource BorderLight}"
                                            BorderThickness="1"
                                            CornerRadius="12"
                                            Margin="0,0,5,0"
                                            VerticalAlignment="Center">
                                        <ToggleButton x:Name="DropDownToggle"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                    ClickMode="Press"
                                                    Focusable="False"
                                                    Width="24"
                                                    Height="24">
                                            <TextBlock Text="▼"
                                                     FontSize="8"
                                                     FontFamily="{DynamicResource PoppinsFontFallback}"
                                                     Foreground="{DynamicResource TextSecondary}"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"/>
                                        </ToggleButton>
                                    </Border>
                                </Grid>
                            </Border>
                            
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=MainBorder}"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource CardBackground}"
                                        BorderBrush="{DynamicResource BorderLight}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource BorderCornerRadius}"
                                        MinWidth="{Binding ActualWidth, ElementName=MainBorder}"
                                        MaxHeight="200">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Direction="270" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                    </Border.Effect>
                                    <ScrollViewer x:Name="DropDownScrollViewer"
                                                VerticalScrollBarVisibility="Auto"
                                                HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource Primary}"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource CardBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource Primary}"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource CardBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource SurfaceBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
                        <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
                        <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Primary}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20,15,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Filters -->
                <RowDefinition Height="Auto"/> <!-- Summary Cards -->
                <RowDefinition Height="Auto"/> <!-- Data Grid -->
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="{DynamicResource DefaultMarginThickness}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Back Button -->
                    <controls:CircularBackButton 
                        Command="{Binding BackToReportsHubCommand}"
                        ButtonSize="30"
                        ButtonBackground="{DynamicResource SurfaceBackground}"
                        ButtonForeground="{DynamicResource TextSecondary}"
                        ButtonHoverBackground="{DynamicResource OverlayBackground}"
                        ButtonHoverForeground="{DynamicResource TextSecondary}"
                        Margin="0,0,10,0"/>

                    <!-- Title -->
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Sales Report" 
                                  FontSize="{DynamicResource FontSizeLarge}"
                                  FontWeight="Bold"
                                  FontFamily="{DynamicResource PoppinsFontFallback}"
                                  Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock FontSize="{DynamicResource FontSizeSmall}"
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,4,0,0">
                            <Run Text="Analyze sales performance and trends •"/>
                            <Run Text="{Binding DateRangeDisplay, FallbackValue='Last 30 Days'}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

                <!-- Refresh Button -->
                <Button Grid.Column="2" 
                       Command="{Binding LoadReportDataCommand}"
                       Style="{StaticResource ActionButtonStyle}"
                       Height="44"
                       Padding="20,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="↻" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Compact Filter Panel - All in One Line with AddGoodsReturn Style -->
            <Border Grid.Row="1" 
                   Style="{StaticResource CardStyle}"
                   Margin="0,0,0,20"
                   Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Quick Date Filters -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Quick:" 
                                  FontWeight="SemiBold" 
                                  FontSize="12"
                                  Foreground="{DynamicResource TextPrimary}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0"/>
                        <Button Content="Today" Command="{Binding SetTodayFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Yesterday" Command="{Binding SetYesterdayFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last 7 Days" Command="{Binding SetLast7DaysFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last 30 Days" Command="{Binding SetLast30DaysFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="This Month" Command="{Binding SetThisMonthFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last Month" Command="{Binding SetLastMonthFilterCommand}" 
                               Style="{StaticResource SmallActionButtonStyle}" Height="28" Padding="10,4" FontSize="12"/>
                    </StackPanel>

                    <!-- All Custom Filters in One Line with FormComboBoxStyle and CustomDatePicker -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Start Date -->
                        <controls:CustomDatePicker Grid.Column="0"
                                                  SelectedDate="{Binding FilterStartDate, Mode=TwoWay}"
                                                  Placeholder="Start Date"
                                                  ToolTip="Start Date"/>

                        <!-- End Date -->
                        <controls:CustomDatePicker Grid.Column="2"
                                                  SelectedDate="{Binding FilterEndDate, Mode=TwoWay}"
                                                  Placeholder="End Date"
                                                  ToolTip="End Date"/>

                        <!-- Payment Method -->
                        <ComboBox Grid.Column="4"
                                 ItemsSource="{Binding PaymentMethods}" 
                                 SelectedItem="{Binding FilterPaymentMethod, Mode=TwoWay}"
                                 DisplayMemberPath="Name"
                                 Style="{StaticResource FormComboBoxStyle}"
                                 ToolTip="Payment Method"/>

                        <!-- Category -->
                        <ComboBox Grid.Column="6"
                                 ItemsSource="{Binding Categories}" 
                                 SelectedItem="{Binding FilterCategory, Mode=TwoWay}"
                                 DisplayMemberPath="Name"
                                 Style="{StaticResource FormComboBoxStyle}"
                                 ToolTip="Category"/>

                        <!-- Status -->
                        <ComboBox Grid.Column="8"
                                 ItemsSource="{Binding Statuses}" 
                                 SelectedItem="{Binding FilterStatus, Mode=TwoWay}"
                                 DisplayMemberPath="Name"
                                 Style="{StaticResource FormComboBoxStyle}"
                                 ToolTip="Status"/>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Column="10" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Button Content="Apply" 
                                   Command="{Binding ApplyFiltersCommand}"
                                   Style="{StaticResource ActionButtonStyle}"
                                   Height="38"
                                   Padding="16,8"
                                   FontSize="12"
                                   Margin="0,0,6,0"/>
                            <Button Content="Reset" 
                                   Command="{Binding ResetFiltersCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Height="38"
                                   Padding="12,8"
                                   FontSize="12"
                                   Margin="0,0,6,0"/>
                            <Button Content="📊 Export" 
                                   Command="{Binding ShowExportMenuCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Height="38"
                                   Padding="12,8"
                                   FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border Grid.Row="0" Grid.RowSpan="5"
                   Background="{DynamicResource OverlayBackground}"
                   Opacity="0.9"
                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Panel.ZIndex="999">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="4"/>
                    <TextBlock Text="Loading report data..." 
                              Foreground="{DynamicResource TextPrimary}" 
                              Margin="0,15,0,0"
                              FontSize="{DynamicResource FontSizeSmall}"/>
                </StackPanel>
            </Border>

            <!-- Summary Cards -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Sales -->
                <Border Grid.Column="0" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Total Sales" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="{Binding Summary.TotalSalesAmount, Converter={StaticResource CurrencyPriceConverter}}" 
                                      FontSize="28" 
                                      FontWeight="Bold"
                                      Foreground="{DynamicResource Primary}"/>
                            <TextBlock Text="{Binding Summary.TotalTransactions, StringFormat='{}{0} transactions'}" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}"
                                      Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Average Transaction -->
                <Border Grid.Column="2" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Avg Transaction" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.AverageTransactionValue, Converter={StaticResource CurrencyPriceConverter}}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource Success}"/>
                    </Grid>
                </Border>

                <!-- Total Items Sold -->
                <Border Grid.Column="4" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Items Sold" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.TotalItemsSold, StringFormat='{}{0:N0}'}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource Info}"/>
                    </Grid>
                </Border>

                <!-- Gross Profit -->
                <Border Grid.Column="6" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Gross Profit" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.GrossProfit, Converter={StaticResource CurrencyPriceConverter}}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource Success}"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Data Grid -->
            <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Grid Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" 
                                  Text="Transactions" 
                                  FontSize="{DynamicResource FontSizeMedium}" 
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource TextPrimary}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Show:" 
                                      FontSize="{DynamicResource FontSizeSmall}"
                                      Foreground="{DynamicResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            <ComboBox SelectedItem="{Binding PageSize, Mode=TwoWay}"
                                     Width="80"
                                     Height="32">
                                <ComboBoxItem Content="25"/>
                                <ComboBoxItem Content="50"/>
                                <ComboBoxItem Content="100"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Transactions DataGrid -->
                    <DataGrid Grid.Row="1" 
                             ItemsSource="{Binding Transactions}"
                             Style="{StaticResource ReportDataGridStyle}"
                             ColumnHeaderStyle="{StaticResource ReportColumnHeaderStyle}"
                             RowStyle="{StaticResource ReportRowStyle}"
                             CellStyle="{StaticResource ReportCellStyle}"
                             MaxHeight="500">
                        
                        <DataGrid.Columns>
                            <!-- Invoice Number -->
                            <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportBoldTextCellStyle}">
                                        <Setter Property="Foreground" Value="#4A90E2"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Date -->
                            <DataGridTextColumn Header="Date" Binding="{Binding SaleDate, StringFormat='dd MMM yyyy'}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Time -->
                            <DataGridTextColumn Header="Time" Binding="{Binding SaleDate, StringFormat='HH:mm'}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Customer -->
                            <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*" MinWidth="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Items Count -->
                            <DataGridTextColumn Header="Items" Binding="{Binding ItemsCount}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Subtotal -->
                            <DataGridTextColumn Header="Subtotal" Binding="{Binding SubTotal, Converter={StaticResource CurrencyPriceConverter}}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Discount -->
                            <DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, Converter={StaticResource CurrencyPriceConverter}}" Width="95">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}">
                                        <Setter Property="Foreground" Value="#E67E22"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Tax -->
                            <DataGridTextColumn Header="Tax" Binding="{Binding TaxAmount, Converter={StaticResource CurrencyPriceConverter}}" Width="90">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Total -->
                            <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, Converter={StaticResource CurrencyPriceConverter}}" Width="125">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Foreground" Value="#1976D2"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Payment Method -->
                            <DataGridTextColumn Header="Payment" Binding="{Binding PaymentMethodName}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <!-- Status -->
                            <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource ReportBoldTextCellStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <Grid Grid.Row="2" Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                  FontSize="{DynamicResource FontSizeSmall}"
                                  Foreground="{DynamicResource TextSecondary}"
                                  VerticalAlignment="Center">
                            <Run Text="Showing"/>
                            <Run Text="{Binding StartRecord, Mode=OneWay}"/>
                            <Run Text="to"/>
                            <Run Text="{Binding EndRecord, Mode=OneWay}"/>
                            <Run Text="of"/>
                            <Run Text="{Binding TotalRecords, Mode=OneWay}"/>
                            <Run Text="transactions"/>
                        </TextBlock>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="← Previous" 
                                   Command="{Binding PreviousPageCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Width="100"
                                   Height="32"
                                   Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding CurrentPage, StringFormat='Page {0}'}" 
                                      FontSize="{DynamicResource FontSizeSmall}"
                                      FontWeight="SemiBold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <Button Content="Next →" 
                                   Command="{Binding NextPageCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Width="100"
                                   Height="32"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
