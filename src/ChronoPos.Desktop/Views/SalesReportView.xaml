<UserControl x:Class="ChronoPos.Desktop.Views.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             Background="{DynamicResource PageBackground}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:CurrencyPriceConverter x:Key="CurrencyPriceConverter"/>
        <converters:LocalizationConverter x:Key="LocalizationConverter"/>
        
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="2" Opacity="0.08" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Summary Card Style -->
        <Style x:Key="SummaryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.06" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="8"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Cursor" Value="No"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="8"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EEEEEE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Input Style -->
        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20,15,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Filters -->
                <RowDefinition Height="Auto"/> <!-- Summary Cards -->
                <RowDefinition Height="Auto"/> <!-- Data Grid -->
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button (consistent with other pages) -->
                <Button Grid.Column="0" 
                       Command="{Binding BackToReportsHubCommand}"
                       Style="{StaticResource SecondaryButtonStyle}"
                       Width="44"
                       Height="44"
                       Padding="0"
                       Margin="0,0,15,0"
                       ToolTip="Back to Reports Hub">
                    <TextBlock Text="←" 
                              FontSize="20" 
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Button>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Sales Report" 
                              FontSize="28" 
                              FontWeight="Bold"
                              Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock FontSize="13" 
                              Foreground="{DynamicResource TextSecondary}"
                              Margin="0,4,0,0">
                        <Run Text="Analyze sales performance and trends •"/>
                        <Run Text="{Binding DateRangeDisplay, FallbackValue='Last 30 Days'}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>

                <Button Grid.Column="2" 
                       Content="↻ Refresh" 
                       Command="{Binding LoadReportDataCommand}"
                       Style="{StaticResource FilterButtonStyle}"
                       Height="44"
                       Padding="20,12"/>
            </Grid>

            <!-- Compact Filter Panel - All in One Line -->
            <Border Grid.Row="1" 
                   Style="{StaticResource CardStyle}"
                   Margin="0,0,0,20"
                   Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Quick Date Filters -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Quick:" 
                                  FontWeight="SemiBold" 
                                  FontSize="12"
                                  Foreground="{DynamicResource TextSecondary}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0"/>
                        <Button Content="Today" Command="{Binding SetTodayFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Yesterday" Command="{Binding SetYesterdayFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last 7 Days" Command="{Binding SetLast7DaysFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last 30 Days" Command="{Binding SetLast30DaysFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="This Month" Command="{Binding SetThisMonthFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Height="28" Padding="10,4" FontSize="12"/>
                        <Button Content="Last Month" Command="{Binding SetLastMonthFilterCommand}" 
                               Style="{StaticResource SecondaryButtonStyle}" Height="28" Padding="10,4" FontSize="12"/>
                    </StackPanel>

                    <!-- All Custom Filters in One Line -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Start Date -->
                        <Border Grid.Column="0" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="8,6">
                            <StackPanel>
                                <TextBlock Text="From" FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,2"/>
                                <DatePicker SelectedDate="{Binding FilterStartDate, Mode=TwoWay}" 
                                           BorderThickness="0"
                                           Background="Transparent"
                                           FontSize="12"
                                           Height="24"/>
                            </StackPanel>
                        </Border>

                        <!-- End Date -->
                        <Border Grid.Column="2" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="8,6">
                            <StackPanel>
                                <TextBlock Text="To" FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,2"/>
                                <DatePicker SelectedDate="{Binding FilterEndDate, Mode=TwoWay}" 
                                           BorderThickness="0"
                                           Background="Transparent"
                                           FontSize="12"
                                           Height="24"/>
                            </StackPanel>
                        </Border>

                        <!-- Payment Method -->
                        <Border Grid.Column="4" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="8,6">
                            <StackPanel>
                                <TextBlock Text="Payment" FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,2"/>
                                <ComboBox ItemsSource="{Binding PaymentMethods}" 
                                         SelectedItem="{Binding FilterPaymentMethod, Mode=TwoWay}"
                                         DisplayMemberPath="Name"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         FontSize="12"
                                         Height="24"
                                         Padding="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Category -->
                        <Border Grid.Column="6" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="8,6">
                            <StackPanel>
                                <TextBlock Text="Category" FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,2"/>
                                <ComboBox ItemsSource="{Binding Categories}" 
                                         SelectedItem="{Binding FilterCategory, Mode=TwoWay}"
                                         DisplayMemberPath="Name"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         FontSize="12"
                                         Height="24"
                                         Padding="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Status -->
                        <Border Grid.Column="8" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="8,6">
                            <StackPanel>
                                <TextBlock Text="Status" FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,2"/>
                                <ComboBox ItemsSource="{Binding Statuses}" 
                                         SelectedItem="{Binding FilterStatus, Mode=TwoWay}"
                                         DisplayMemberPath="Name"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         FontSize="12"
                                         Height="24"
                                         Padding="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Column="10" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Button Content="Apply" 
                                   Command="{Binding ApplyFiltersCommand}"
                                   Style="{StaticResource FilterButtonStyle}"
                                   Height="34"
                                   Padding="16,8"
                                   FontSize="12"
                                   Margin="0,0,6,0"/>
                            <Button Content="Reset" 
                                   Command="{Binding ResetFiltersCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Height="34"
                                   Padding="12,8"
                                   FontSize="12"
                                   Margin="0,0,6,0"/>
                            <Button Content="📊 Export" 
                                   Command="{Binding ShowExportMenuCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Height="34"
                                   Padding="12,8"
                                   FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border Grid.Row="0" Grid.RowSpan="5"
                   Background="#80000000"
                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Panel.ZIndex="999">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="4"/>
                    <TextBlock Text="Loading report data..." 
                              Foreground="White" 
                              Margin="0,15,0,0"
                              FontSize="14"/>
                </StackPanel>
            </Border>

            <!-- Summary Cards -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Sales -->
                <Border Grid.Column="0" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Total Sales" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="{Binding Summary.TotalSalesAmount, Converter={StaticResource CurrencyPriceConverter}}" 
                                      FontSize="28" 
                                      FontWeight="Bold"
                                      Foreground="#4A90E2"/>
                            <TextBlock Text="{Binding Summary.TotalTransactions, StringFormat='{}{0} transactions'}" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}"
                                      Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Average Transaction -->
                <Border Grid.Column="2" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Avg Transaction" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.AverageTransactionValue, Converter={StaticResource CurrencyPriceConverter}}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="#50C878"/>
                    </Grid>
                </Border>

                <!-- Total Items Sold -->
                <Border Grid.Column="4" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Items Sold" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.TotalItemsSold, StringFormat='{}{0:N0}'}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="#FFB84D"/>
                    </Grid>
                </Border>

                <!-- Gross Profit -->
                <Border Grid.Column="6" Style="{StaticResource SummaryCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Gross Profit" 
                                  FontSize="12" 
                                  Foreground="{DynamicResource TextSecondary}"
                                  Margin="0,0,0,8"/>
                        <TextBlock Grid.Row="1" 
                                  Text="{Binding Summary.GrossProfit, Converter={StaticResource CurrencyPriceConverter}}" 
                                  FontSize="28" 
                                  FontWeight="Bold"
                                  Foreground="#27AE60"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Data Grid -->
            <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Grid Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" 
                                  Text="Transactions" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource TextPrimary}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Show:" 
                                      FontSize="13"
                                      Foreground="{DynamicResource TextSecondary}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            <ComboBox SelectedItem="{Binding PageSize, Mode=TwoWay}"
                                     Width="80"
                                     Height="32">
                                <ComboBoxItem Content="25"/>
                                <ComboBoxItem Content="50"/>
                                <ComboBoxItem Content="100"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Transactions DataGrid -->
                    <DataGrid Grid.Row="1" 
                             ItemsSource="{Binding Transactions}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="None"
                             HeadersVisibility="Column"
                             RowHeight="56"
                             BorderThickness="0"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserResizeRows="False"
                             SelectionMode="Single"
                             SelectionUnit="FullRow"
                             Background="White"
                             AlternatingRowBackground="#F9FAFB"
                             MaxHeight="500"
                             FontSize="13">
                        
                        <!-- DataGrid Styles -->
                        <DataGrid.Resources>
                            <!-- Header Style -->
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Foreground" Value="#495057"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E9ECEF"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                            
                            <!-- Row Style -->
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#F1F3F5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F7FF"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            
                            <!-- Cell Style -->
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="16,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}"
                                                   Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        
                        <DataGrid.Columns>
                            <!-- Invoice Number -->
                            <DataGridTemplateColumn Header="Invoice #" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding InvoiceNumber}" 
                                                  FontWeight="SemiBold"
                                                  Foreground="#4A90E2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Date with Icon -->
                            <DataGridTemplateColumn Header="Date and Time" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding SaleDate, StringFormat='dd MMM yyyy'}" 
                                                      FontWeight="Medium"
                                                      Foreground="{DynamicResource TextPrimary}"/>
                                            <TextBlock Text="{Binding SaleDate, StringFormat='HH:mm'}" 
                                                      FontSize="11"
                                                      Foreground="{DynamicResource TextSecondary}"
                                                      Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Customer -->
                            <DataGridTemplateColumn Header="Customer" Width="*" MinWidth="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding CustomerName}" 
                                                      FontWeight="Medium"
                                                      Foreground="{DynamicResource TextPrimary}"
                                                      TextTrimming="CharacterEllipsis"/>
                                            <TextBlock FontSize="11" 
                                                      Foreground="{DynamicResource TextSecondary}"
                                                      Margin="0,2,0,0">
                                                <Run Text="{Binding ItemsCount}"/>
                                                <Run Text="items"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Subtotal -->
                            <DataGridTemplateColumn Header="Subtotal" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SubTotal, Converter={StaticResource CurrencyPriceConverter}}" 
                                                  TextAlignment="Right"
                                                  Foreground="{DynamicResource TextPrimary}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Discount -->
                            <DataGridTemplateColumn Header="Discount" Width="95">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DiscountAmount, Converter={StaticResource CurrencyPriceConverter}}" 
                                                  TextAlignment="Right"
                                                  Foreground="#E67E22"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Tax -->
                            <DataGridTemplateColumn Header="Tax" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TaxAmount, Converter={StaticResource CurrencyPriceConverter}}" 
                                                  TextAlignment="Right"
                                                  Foreground="{DynamicResource TextSecondary}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Total (Prominent) -->
                            <DataGridTemplateColumn Header="Total Amount" Width="125">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#F0F7FF" 
                                               CornerRadius="6"
                                               Padding="8,6"
                                               HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding TotalAmount, Converter={StaticResource CurrencyPriceConverter}}" 
                                                      TextAlignment="Right" 
                                                      FontWeight="Bold"
                                                      FontSize="14"
                                                      Foreground="#1976D2"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Payment Method -->
                            <DataGridTemplateColumn Header="Payment" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding PaymentMethodName, Converter={StaticResource StatusToColorConverter}}"
                                               CornerRadius="4"
                                               Padding="6,3"
                                               Opacity="0.15"
                                               HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding PaymentMethodName}" 
                                                      FontSize="11"
                                                      FontWeight="SemiBold"
                                                      Foreground="{DynamicResource TextPrimary}"
                                                      HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                            <!-- Status Badge -->
                            <DataGridTemplateColumn Header="Status" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusName, Converter={StaticResource StatusToColorConverter}}"
                                               CornerRadius="14"
                                               Padding="10,5"
                                               HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding StatusName}" 
                                                      FontSize="11"
                                                      FontWeight="SemiBold"
                                                      Foreground="White"
                                                      HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <Grid Grid.Row="2" Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                                  FontSize="13"
                                  Foreground="{DynamicResource TextSecondary}"
                                  VerticalAlignment="Center">
                            <Run Text="Showing"/>
                            <Run Text="{Binding StartRecord, Mode=OneWay}"/>
                            <Run Text="to"/>
                            <Run Text="{Binding EndRecord, Mode=OneWay}"/>
                            <Run Text="of"/>
                            <Run Text="{Binding TotalRecords, Mode=OneWay}"/>
                            <Run Text="transactions"/>
                        </TextBlock>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="← Previous" 
                                   Command="{Binding PreviousPageCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Width="100"
                                   Height="32"
                                   Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding CurrentPage, StringFormat='Page {0}'}" 
                                      FontSize="13"
                                      FontWeight="SemiBold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <Button Content="Next →" 
                                   Command="{Binding NextPageCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Width="100"
                                   Height="32"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
