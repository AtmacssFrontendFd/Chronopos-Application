<UserControl x:Class="ChronoPos.Desktop.Views.Controls.CustomDatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="38" d:DesignWidth="160">
    
    <Border Background="{DynamicResource SurfaceBackground}"
            BorderBrush="{DynamicResource BorderLight}"
            BorderThickness="1"
            CornerRadius="{DynamicResource BorderCornerRadius}"
            x:Name="MainBorder">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- DatePicker without visible border -->
            <DatePicker x:Name="InnerDatePicker"
                       Grid.Column="0"
                       BorderThickness="0"
                       Background="Transparent"
                       Foreground="{DynamicResource TextSecondary}"
                       FontSize="{DynamicResource FontSizeSmall}"
                       FontFamily="{DynamicResource PoppinsFontFallback}"
                       Padding="12,8"
                       VerticalContentAlignment="Center"
                       SelectedDateChanged="InnerDatePicker_SelectedDateChanged"/>

            <!-- Circular Calendar Icon Button -->
            <Border Grid.Column="1"
                    Width="24"
                    Height="24"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource BorderLight}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Cursor="Hand"
                    MouseLeftButtonDown="CalendarIcon_Click">
                <TextBlock Text="📅"
                          FontSize="12"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          IsHitTestVisible="False"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
