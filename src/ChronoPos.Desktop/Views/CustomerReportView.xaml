<UserControl x:Class="ChronoPos.Desktop.Views.CustomerReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             xmlns:controls="clr-namespace:ChronoPos.Desktop.Views.Controls"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             Background="{DynamicResource MainBackground}"
             FontFamily="{DynamicResource PoppinsFontFallback}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:CurrencyPriceConverter x:Key="CurrencyPriceConverter"/>
        <converters:LocalizationConverter x:Key="LocalizationConverter"/>
        
        <!-- Card Style - Theme Compatible -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Margin" Value="{DynamicResource DefaultMarginThickness}"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
        </Style>

        <!-- Summary Card Style - Theme Compatible -->
        <Style x:Key="SummaryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>

        <!-- Action Button Style - Theme Compatible -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource BorderCornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDarker}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BorderLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style - Theme Compatible -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource BorderCornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Compact Filter Button Style -->
        <Style x:Key="CompactFilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input Style -->
        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
        </Style>

        <!-- Form ComboBox Style - Circular Dropdown Icon -->
        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{DynamicResource BorderCornerRadius}"
                                    MinHeight="{TemplateBinding MinHeight}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Selected Item Display -->
                                    <ContentPresenter Grid.Column="0"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    Margin="{TemplateBinding Padding}"/>
                                    
                                    <!-- Circular Dropdown Toggle -->
                                    <Border Grid.Column="1"
                                            x:Name="DropdownToggle"
                                            Width="24"
                                            Height="24"
                                            Background="{DynamicResource BorderLight}"
                                            CornerRadius="12"
                                            Margin="0,0,8,0">
                                        <TextBlock Text="▼"
                                                 FontSize="8"
                                                 Foreground="{DynamicResource TextSecondary}"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                            
                            <!-- Popup -->
                            <Popup x:Name="PART_Popup"
                                   PlacementTarget="{Binding ElementName=MainBorder}"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource CardBackground}"
                                        BorderBrush="{DynamicResource BorderLight}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource BorderCornerRadius}"
                                        MinWidth="{Binding ActualWidth, ElementName=MainBorder}"
                                        MaxHeight="300"
                                        Margin="0,4,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" 
                                                        ShadowDepth="2" 
                                                        Opacity="0.3" 
                                                        Color="Black"/>
                                    </Border.Effect>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource Primary}"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource CardBackground}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource Primary}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="DropdownToggle" Property="Background" Value="{DynamicResource Primary}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource OverlayBackground}"/>
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Primary}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Loading Overlay - Theme Compatible -->
        <Border Background="{DynamicResource OverlayBackground}" 
                Opacity="0.9"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⟳" FontSize="48" Foreground="{DynamicResource Primary}" HorizontalAlignment="Center">
                    <TextBlock.RenderTransform>
                        <RotateTransform x:Name="LoadingRotation" CenterX="24" CenterY="24"/>
                    </TextBlock.RenderTransform>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                   Storyboard.TargetProperty="Angle"
                                                   From="0" To="360" Duration="0:0:1.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
                <TextBlock Text="Loading Customer Report..." 
                          FontSize="{DynamicResource FontSizeMedium}" 
                          Foreground="{DynamicResource TextPrimary}"
                          FontFamily="{DynamicResource PoppinsFontFallback}"
                          Margin="0,16,0,0"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="24">
                
                <!-- Header with Back Button and Title -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Back Button and Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- CircularBackButton Control -->
                        <controls:CircularBackButton 
                            Command="{Binding BackToReportsHubCommand}"
                            ButtonSize="30"
                            ButtonBackground="{DynamicResource SurfaceBackground}"
                            ButtonForeground="{DynamicResource TextSecondary}"
                            ButtonHoverBackground="{DynamicResource OverlayBackground}"
                            ButtonHoverForeground="{DynamicResource TextSecondary}"
                            Margin="0,0,15,0"/>
                        
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Customer Report" 
                                      FontSize="{DynamicResource FontSizeLarge}" 
                                      FontWeight="Bold" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="{Binding DateRangeDisplay}" 
                                      FontSize="{DynamicResource FontSizeSmall}" 
                                      Foreground="{DynamicResource TextSecondary}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Export Buttons -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="📄 Excel" 
                               Command="{Binding ExportToExcelCommand}"
                               Style="{StaticResource SecondaryButtonStyle}"
                               Height="34"
                               Padding="12,8"
                               FontSize="12"
                               Margin="0,0,6,0"/>
                        <Button Content="📊 PDF" 
                               Command="{Binding ExportToPdfCommand}"
                               Style="{StaticResource SecondaryButtonStyle}"
                               Height="34"
                               Padding="12,8"
                               FontSize="12"/>
                    </StackPanel>
                </Grid>

                <!-- Compact Filters Section with Quick Filters Inside -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <!-- Quick Date Filters -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                      Text="Quick Filters:" 
                                      FontSize="{DynamicResource FontSizeSmall}" 
                                      FontWeight="SemiBold" 
                                      Foreground="{DynamicResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,12,0"/>
                            
                            <WrapPanel Grid.Column="1">
                                <Button Content="Today" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetTodayFilterCommand}"
                                       Margin="0,0,6,0"/>
                                <Button Content="Yesterday" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetYesterdayFilterCommand}"
                                       Margin="0,0,6,0"/>
                                <Button Content="Last 7 Days" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetLast7DaysFilterCommand}"
                                       Margin="0,0,6,0"/>
                                <Button Content="Last 30 Days" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetLast30DaysFilterCommand}"
                                       Margin="0,0,6,0"/>
                                <Button Content="This Month" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetThisMonthFilterCommand}"
                                       Margin="0,0,6,0"/>
                                <Button Content="Last Month" 
                                       Style="{StaticResource CompactFilterButtonStyle}" 
                                       Command="{Binding SetLastMonthFilterCommand}"/>
                            </WrapPanel>
                        </Grid>

                        <!-- Separator -->
                        <Border Height="1" Background="{DynamicResource BorderLight}" Margin="0,0,0,12"/>

                        <!-- Compact Filter Grid - Single Row -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Start Date -->
                            <controls:CustomDatePicker Grid.Column="0"
                                                      SelectedDate="{Binding FilterStartDate, Mode=TwoWay}"
                                                      Placeholder="Start Date"
                                                      ToolTip="Start Date"/>

                            <!-- End Date -->
                            <controls:CustomDatePicker Grid.Column="2"
                                                      SelectedDate="{Binding FilterEndDate, Mode=TwoWay}"
                                                      Placeholder="End Date"
                                                      ToolTip="End Date"/>

                            <!-- Search -->
                            <TextBox Grid.Column="4"
                                    Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Style="{StaticResource InputStyle}"
                                    ToolTip="Search Customer"/>

                            <!-- Status Filter -->
                            <ComboBox Grid.Column="6"
                                     SelectedValue="{Binding FilterIsActive, Mode=TwoWay}"
                                     Style="{StaticResource FormComboBoxStyle}"
                                     ToolTip="Customer Status">
                                <ComboBoxItem Content="All Customers" IsSelected="True"/>
                                <ComboBoxItem Content="Active Only" Tag="True"/>
                                <ComboBoxItem Content="Inactive Only" Tag="False"/>
                            </ComboBox>

                            <!-- Segment Filter -->
                            <ComboBox Grid.Column="8"
                                     SelectedValue="{Binding FilterSegment, Mode=TwoWay}"
                                     Style="{StaticResource FormComboBoxStyle}"
                                     ToolTip="Customer Segment">
                                <ComboBoxItem Content="All Segments" IsSelected="True"/>
                                <ComboBoxItem Content="VIP" Tag="VIP"/>
                                <ComboBoxItem Content="Premium" Tag="Premium"/>
                                <ComboBoxItem Content="Loyal" Tag="Loyal"/>
                                <ComboBoxItem Content="Regular" Tag="Regular"/>
                                <ComboBoxItem Content="New" Tag="New"/>
                            </ComboBox>

                            <!-- Apply Filter Button -->
                            <Button Grid.Column="10"
                                   Content="Apply"
                                   Style="{StaticResource ActionButtonStyle}"
                                   Command="{Binding ApplyFiltersCommand}"
                                   Height="38"
                                   Padding="16,8"/>

                            <!-- Reset Button -->
                            <Button Grid.Column="12"
                                   Content="Reset"
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Command="{Binding ResetFiltersCommand}"
                                   Height="38"
                                   Padding="16,8"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Summary Cards - Theme Compatible -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Customers Card -->
                    <Border Grid.Column="0" Style="{StaticResource SummaryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="👥" FontSize="20" Margin="0,0,0,6"/>
                            <TextBlock Text="Total Customers" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Summary.TotalCustomers}" 
                                      FontSize="22" 
                                      FontWeight="Bold" 
                                      Foreground="{DynamicResource TextPrimary}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"/>
                            <TextBlock FontSize="10" 
                                      Foreground="{DynamicResource Success}" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,4,0,0">
                                <Run Text="+"/>
                                <Run Text="{Binding Summary.NewCustomersThisPeriod}"/>
                                <Run Text=" new"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Active Customers Card -->
                    <Border Grid.Column="2" Style="{StaticResource SummaryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="✅" FontSize="20" Margin="0,0,0,6"/>
                            <TextBlock Text="Active Customers" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Summary.ActiveCustomers}" 
                                      FontSize="22" 
                                      FontWeight="Bold" 
                                      Foreground="{DynamicResource Success}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"/>
                        </StackPanel>
                    </Border>

                    <!-- Total Revenue Card -->
                    <Border Grid.Column="4" Style="{StaticResource SummaryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="💰" FontSize="20" Margin="0,0,0,6"/>
                            <TextBlock Text="Total Revenue" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Summary.TotalRevenue, Converter={StaticResource CurrencyPriceConverter}}" 
                                      FontSize="22" 
                                      FontWeight="Bold" 
                                      Foreground="{DynamicResource Info}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"/>
                        </StackPanel>
                    </Border>

                    <!-- Average Order Value Card -->
                    <Border Grid.Column="6" Style="{StaticResource SummaryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="📊" FontSize="20" Margin="0,0,0,6"/>
                            <TextBlock Text="Avg. Order Value" 
                                      FontSize="11" 
                                      Foreground="{DynamicResource TextSecondary}" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Summary.AverageOrderValue, Converter={StaticResource CurrencyPriceConverter}}" 
                                      FontSize="22" 
                                      FontWeight="Bold" 
                                      Foreground="{DynamicResource Warning}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Customers Table -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                      Text="Customer Details" 
                                      FontSize="{DynamicResource FontSizeMedium}" 
                                      FontWeight="SemiBold" 
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Foreground="{DynamicResource TextPrimary}"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="{Binding StartRecord}" 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text=" - " 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="{Binding EndRecord}" 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text=" of " 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text="{Binding TotalRecords}" 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                                <TextBlock Text=" customers" 
                                          FontSize="{DynamicResource FontSizeSmall}" 
                                          FontFamily="{DynamicResource PoppinsFontFallback}"
                                          Foreground="{DynamicResource TextSecondary}"/>
                            </StackPanel>
                        </Grid>

                        <!-- Modern DataGrid -->
                        <DataGrid ItemsSource="{Binding Customers}"
                                 Style="{StaticResource ReportDataGridStyle}"
                                 ColumnHeaderStyle="{StaticResource ReportColumnHeaderStyle}"
                                 RowStyle="{StaticResource ReportRowStyle}"
                                 CellStyle="{StaticResource ReportCellStyle}">
                            
                            <DataGrid.Columns>
                                <!-- Customer Name -->
                                <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportBoldTextCellStyle}"/>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Email -->
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}">
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Phone -->
                                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Total Purchases -->
                                <DataGridTextColumn Header="Purchases" Binding="{Binding TotalPurchases}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="#1976D2"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Total Revenue -->
                                <DataGridTextColumn Header="Revenue" Binding="{Binding TotalRevenue, Converter={StaticResource CurrencyPriceConverter}}" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="#28a745"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Average Order Value -->
                                <DataGridTextColumn Header="Avg. Order" Binding="{Binding AverageOrderValue, Converter={StaticResource CurrencyPriceConverter}}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportNumericCellStyle}"/>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Segment -->
                                <DataGridTextColumn Header="Segment" Binding="{Binding CustomerSegment}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="#F57C00"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Last Purchase Date -->
                                <DataGridTextColumn Header="Last Purchase" Binding="{Binding LastPurchaseDate, StringFormat='dd MMM yyyy'}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ReportTextCellStyle}"/>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Pagination - Theme Compatible -->
                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" 
                                      VerticalAlignment="Center"
                                      FontSize="{DynamicResource FontSizeSmall}"
                                      FontFamily="{DynamicResource PoppinsFontFallback}"
                                      Foreground="{DynamicResource TextSecondary}">
                                <Run Text="Page"/>
                                <Run Text="{Binding CurrentPage}" FontWeight="SemiBold"/>
                                <Run Text="of"/>
                                <Run Text="{Binding TotalPages}" FontWeight="SemiBold"/>
                            </TextBlock>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="◀ Previous" 
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Command="{Binding PreviousPageCommand}"
                                       IsEnabled="{Binding CanGoPrevious}"
                                       Padding="16,8"
                                       Height="32"/>
                                <Button Content="Next ▶" 
                                       Style="{StaticResource ActionButtonStyle}"
                                       Command="{Binding NextPageCommand}"
                                       IsEnabled="{Binding CanGoNext}"
                                       Padding="16,8"
                                       Height="32"
                                       Margin="8,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
