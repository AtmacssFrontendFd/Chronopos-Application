<UserControl x:Class="ChronoPos.Desktop.Views.RestaurantTableSidePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="450"
             Background="White">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Form Styles -->
        <Style TargetType="TextBox" x:Key="FormTextBoxStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="FormComboBoxStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <!-- Header -->
                <TextBlock Text="{Binding Title}" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Margin="0,0,0,24"
                          Foreground="#333"/>

                <!-- Validation Error Message -->
                <Border Background="#FFF3CD" 
                        BorderBrush="#FFA500" 
                        BorderThickness="1" 
                        CornerRadius="4" 
                        Padding="12,8"
                        Margin="0,0,0,16"
                        Visibility="{Binding HasValidationError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ValidationMessage}" 
                               FontSize="13" 
                               Foreground="#856404"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Table Number -->
                <TextBlock Text="Table Number *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#555"/>
                <TextBox Text="{Binding TableNumber, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBoxStyle}"
                         Margin="0,0,0,16"/>

                <!-- Location -->
                <TextBlock Text="Location *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#555"/>
                <ComboBox ItemsSource="{Binding LocationOptions}"
                          Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          Style="{StaticResource FormComboBoxStyle}"
                          Margin="0,0,0,16"/>

                <!-- Capacity -->
                <TextBlock Text="Capacity *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#555"/>
                <TextBox Text="{Binding Capacity, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBoxStyle}"
                         Margin="0,0,0,16"/>

                <!-- Status -->
                <TextBlock Text="Status *" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#555"/>
                <ComboBox ItemsSource="{Binding StatusOptions}"
                          SelectedItem="{Binding Status}"
                          Style="{StaticResource FormComboBoxStyle}"
                          Margin="0,0,0,24"/>

                <!-- Action Buttons -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Height="42"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Background="#F5F5F5"
                            Foreground="#666"
                            BorderThickness="1"
                            BorderBrush="#E0E0E0"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="1"
                            Content="Save Table"
                            Command="{Binding SaveCommand}"
                            Height="42"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            Margin="8,0,0,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#BDBDBD"/>
                                        <Setter Property="Foreground" Value="#666"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Loading Indicator -->
                <Border Background="#80000000" 
                        Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}"
                        VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Saving..." 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
