<UserControl x:Class="ChronoPos.Desktop.Views.TaxTypesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource MainBackground}"
             FontFamily="{DynamicResource PoppinsFontFallback}">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:FlowDirectionToGridColumnConverter x:Key="FlowDirectionToGridColumnConverter"/>
        <converters:FlowDirectionToHorizontalAlignmentConverter x:Key="FlowDirectionToHorizontalAlignmentConverter"/>
        <converters:BoolToGridLengthConverter x:Key="BoolToGridLengthConverter"/>
        <converters:BooleanToStringConverter x:Key="BooleanToStringConverter"/>

        <!-- Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource BorderCornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDarker}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BorderLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource Danger}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="TaxTypeCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Margin" Value="{DynamicResource DefaultMarginThickness}"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Main Content - Always full width -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button Grid.Column="0"
                        Content="← Back"
                        Command="{Binding GoBackCommand}"
                        Height="36"
                        MinWidth="80"
                        Margin="0,0,15,0"
                        VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FF6B35"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="#E55A2B"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FF7A47"/>
                                                <Setter Property="BorderBrush" Value="#FF6B35"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#D44B1F"/>
                                                <Setter Property="BorderBrush" Value="#C2441C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Title -->
                <TextBlock Grid.Column="1"
                          Text="Tax Types"
                          FontSize="28"
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextPrimary}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left"/>

                <!-- Refresh Button -->
                <Button Grid.Column="2"
                        Content="🔄 Refresh"
                        Command="{Binding RefreshCommand}"
                        Height="36"
                        MinWidth="120"
                        ToolTip="Refresh tax types">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FFD700"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="BorderBrush" Value="#FFC125"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FFED4A"/>
                                                <Setter Property="BorderBrush" Value="#FFD700"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#E6C200"/>
                                                <Setter Property="BorderBrush" Value="#CC9900"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Search and Filter Section -->
            <Border Grid.Row="1" 
                    Background="{DynamicResource CardBackground}" 
                    CornerRadius="{DynamicResource BorderCornerRadius}"
                    Margin="0,0,0,20">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Section -->
                    <Border Grid.Column="0" 
                            Background="{DynamicResource InputBackground}"
                            BorderBrush="{DynamicResource BorderLight}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Margin="0,0,15,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search Icon -->
                            <TextBlock Grid.Column="0"
                                      Text="🔍"
                                      FontSize="14"
                                      Foreground="{DynamicResource TextSecondary}"
                                      VerticalAlignment="Center"
                                      Margin="12,0,8,0"/>

                            <!-- Search TextBox -->
                            <TextBox Grid.Column="1"
                                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontSize="14"
                                    Padding="0,12"
                                    VerticalContentAlignment="Center"
                                    Foreground="{DynamicResource TextPrimary}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background" Value="Transparent"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                            <!-- Placeholder Text -->
                            <TextBlock Grid.Column="1"
                                      Text="Search tax types..."
                                      FontSize="14"
                                      Foreground="{DynamicResource TextSecondary}"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Clear Search Button -->
                            <Button Grid.Column="2"
                                   Command="{Binding ClearSearchCommand}"
                                   Background="Transparent"
                                   BorderThickness="0"
                                   Padding="8"
                                   Margin="0,0,4,0"
                                   Cursor="Hand"
                                   Visibility="{Binding SearchText, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="✕" 
                                          FontSize="12" 
                                          Foreground="{DynamicResource TextSecondary}"/>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Active Filter Toggle -->
                    <Button Grid.Column="1"
                            Content="{Binding ActiveFilterButtonText}"
                            Command="{Binding ToggleActiveFilterCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            Margin="0,0,15,0"
                            MinWidth="120"/>

                    <!-- Add New Button -->
                    <Button Grid.Column="2"
                            Content="+ Add Tax Type"
                            Command="{Binding ShowAddTaxTypeOverlayCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>
                </Grid>
            </Border>

            <!-- Tax Types DataGrid -->
            <Border Grid.Row="2" 
                    Background="{DynamicResource CardBackground}" 
                    CornerRadius="{DynamicResource BorderCornerRadius}">
                <DataGrid ItemsSource="{Binding FilteredTaxTypes}"
                          SelectedItem="{Binding SelectedTaxType}"
                          Style="{DynamicResource DataGridStyle}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          Margin="20">
                    <DataGrid.Columns>
                        <!-- Name Column -->
                        <DataGridTextColumn Header="Name" 
                                          Binding="{Binding Name}"
                                          Width="200"/>

                        <!-- Description Column -->
                        <DataGridTextColumn Header="Description" 
                                          Binding="{Binding Description}"
                                          Width="250"/>

                        <!-- Value Column -->
                        <DataGridTextColumn Header="Value" 
                                          Binding="{Binding Value, StringFormat=N4}"
                                          Width="100"/>

                        <!-- Type Column -->
                        <DataGridTextColumn Header="Type" 
                                          Binding="{Binding IsPercentage, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Percentage;Fixed Amount'}"
                                          Width="120"/>

                        <!-- Applies To Column -->
                        <DataGridTemplateColumn Header="Applies To" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="Buying" 
                                                   Visibility="{Binding AppliesToBuying, Converter={StaticResource BoolToVisibilityConverter}}"
                                                   FontSize="11" 
                                                   Foreground="Green"/>
                                        <TextBlock Text="Selling" 
                                                   Visibility="{Binding AppliesToSelling, Converter={StaticResource BoolToVisibilityConverter}}"
                                                   FontSize="11" 
                                                   Foreground="Blue"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Status Column -->
                        <DataGridTextColumn Header="Status" 
                                          Binding="{Binding IsActive, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Active;Inactive'}"
                                          Width="100"/>

                        <!-- Actions Column -->
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Edit"
                                               Command="{Binding DataContext.ShowEditTaxTypeOverlayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"
                                               Style="{DynamicResource ModernButtonStyle}"
                                               Background="{DynamicResource Warning}"
                                               Margin="0,0,5,0"
                                               Padding="8,4"
                                               FontSize="11"/>
                                        <Button Content="Delete"
                                               Command="{Binding DataContext.DeleteTaxTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"
                                               Style="{DynamicResource ModernButtonStyle}"
                                               Background="{DynamicResource Danger}"
                                               Padding="8,4"
                                               FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Overlay Background when side panel is open -->
        <Border Background="Black" 
                Opacity="0.3"
                Panel.ZIndex="999"
                Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding CloseOverlayCommand}"/>
            </Border.InputBindings>
        </Border>

        <!-- Side Panel Overlay - Direct child of root Grid for proper z-index -->
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Width="450"
                Background="{DynamicResource CardBackground}"
                BorderThickness="1,0,0,0"
                BorderBrush="{DynamicResource BorderLight}"
                Panel.ZIndex="1000"
                Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform X="450"/>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                                       From="450"
                                                       To="0"
                                                       Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                                       From="0"
                                                       To="450"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <!-- Side Panel Content -->
            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form Header -->
                <TextBlock Grid.Row="0"
                          Text="{Binding FormTitle}"
                          FontSize="20"
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextPrimary}"
                          Margin="0,0,0,25"
                          HorizontalAlignment="Center"/>

                <!-- Form Fields -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Name Field -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Tax Type Name*"
                                      FontWeight="Medium"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,8"
                                      HorizontalAlignment="Left"/>
                            <Border Background="{DynamicResource InputBackground}"
                                    BorderBrush="{DynamicResource BorderLight}"
                                    BorderThickness="1"
                                    CornerRadius="{DynamicResource BorderCornerRadius}">
                                <TextBox Text="{Binding EditingTaxType.Name, UpdateSourceTrigger=PropertyChanged}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="12"
                                        FontSize="14"
                                        Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                        </StackPanel>

                        <!-- Description Field -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Description"
                                      FontWeight="Medium"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,8"
                                      HorizontalAlignment="Left"/>
                            <Border Background="{DynamicResource InputBackground}"
                                    BorderBrush="{DynamicResource BorderLight}"
                                    BorderThickness="1"
                                    CornerRadius="{DynamicResource BorderCornerRadius}">
                                <TextBox Text="{Binding EditingTaxType.Description, UpdateSourceTrigger=PropertyChanged}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="12"
                                        FontSize="14"
                                        Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                        </StackPanel>

                        <!-- Tax Value Field -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Tax Value*"
                                      FontWeight="Medium"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,8"
                                      HorizontalAlignment="Left"/>
                            <Border Background="{DynamicResource InputBackground}"
                                    BorderBrush="{DynamicResource BorderLight}"
                                    BorderThickness="1"
                                    CornerRadius="{DynamicResource BorderCornerRadius}">
                                <TextBox Text="{Binding EditingTaxType.Value, UpdateSourceTrigger=PropertyChanged}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="12"
                                        FontSize="14"
                                        Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                        </StackPanel>

                        <!-- Calculation Order Field -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Calculation Order"
                                      FontWeight="Medium"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,8"
                                      HorizontalAlignment="Left"/>
                            <Border Background="{DynamicResource InputBackground}"
                                    BorderBrush="{DynamicResource BorderLight}"
                                    BorderThickness="1"
                                    CornerRadius="{DynamicResource BorderCornerRadius}">
                                <TextBox Text="{Binding EditingTaxType.CalculationOrder, UpdateSourceTrigger=PropertyChanged}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="12"
                                        FontSize="14"
                                        Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                        </StackPanel>

                        <!-- Tax Options -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Tax Options"
                                      FontWeight="Medium"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,15"/>
                            
                            <StackPanel>
                                <CheckBox Content="Is Percentage"
                                         IsChecked="{Binding EditingTaxType.IsPercentage}"
                                         Margin="0,0,0,10"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextPrimary}"/>
                                
                                <CheckBox Content="Included in Price"
                                         IsChecked="{Binding EditingTaxType.IncludedInPrice}"
                                         Margin="0,0,0,10"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextPrimary}"/>
                                
                                <CheckBox Content="Applies to Buying"
                                         IsChecked="{Binding EditingTaxType.AppliesToBuying}"
                                         Margin="0,0,0,10"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextPrimary}"/>
                                
                                <CheckBox Content="Applies to Selling"
                                         IsChecked="{Binding EditingTaxType.AppliesToSelling}"
                                         Margin="0,0,0,10"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextPrimary}"/>
                                
                                <CheckBox Content="Is Active"
                                         IsChecked="{Binding EditingTaxType.IsActive}"
                                         FontSize="14"
                                         Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!-- Form Actions -->
                <StackPanel Grid.Row="2" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Right"
                           Margin="0,25,0,0">
                    <Button Content="Cancel"
                           Command="{Binding CloseOverlayCommand}"
                           Background="{DynamicResource TextSecondary}"
                           Foreground="White"
                           Style="{DynamicResource ModernButtonStyle}"
                           Margin="0,0,10,0"
                           MinWidth="80"/>
                    <Button Content="Save"
                           Command="{Binding SaveTaxTypeCommand}"
                           Style="{DynamicResource PrimaryButtonStyle}"
                           MinWidth="80"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>