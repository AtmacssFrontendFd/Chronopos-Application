<UserControl x:Class="ChronoPos.Desktop.Views.ReportsHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             Background="{DynamicResource PageBackground}">

    <UserControl.Resources>
        <!-- Report Card Style -->
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="2" Opacity="0.08" Direction="270"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.15" Direction="270"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Margin" Value="0,-2,0,2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Icon Circle Style -->
        <Style x:Key="IconCircleStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="0" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Cards -->
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="Reports" 
                          FontSize="32" 
                          FontWeight="Bold"
                          Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text="Access comprehensive business reports and analytics" 
                          FontSize="14" 
                          Foreground="{DynamicResource TextSecondary}"
                          Margin="0,6,0,0"/>
            </StackPanel>

            <!-- Report Cards Grid -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sales Report Card -->
                <Border Grid.Row="0" Grid.Column="0" 
                       Style="{StaticResource ReportCardStyle}"
                       MouseLeftButtonDown="SalesReportCard_Click"
                       Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Icon -->
                        <Grid Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <Ellipse Style="{StaticResource IconCircleStyle}">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#4A90E2" Offset="0"/>
                                        <GradientStop Color="#357ABD" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="📊" 
                                      FontSize="36" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Content -->
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="Sales Report" 
                                      FontSize="24" 
                                      FontWeight="Bold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="View comprehensive sales analytics including daily, monthly, and yearly reports. Analyze product performance, payment methods, and customer insights."
                                      FontSize="13"
                                      Foreground="{DynamicResource TextSecondary}"
                                      TextWrapping="Wrap"
                                      LineHeight="20"/>
                        </StackPanel>

                        <!-- Action Button -->
                        <Button Grid.Row="2" 
                               Content="View Report →"
                               HorizontalAlignment="Left"
                               Background="Transparent"
                               BorderThickness="0"
                               Foreground="#4A90E2"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Padding="0,10,0,0"
                               Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#357ABD"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Inventory Report Card -->
                <Border Grid.Row="0" Grid.Column="2" 
                       Style="{StaticResource ReportCardStyle}"
                       MouseLeftButtonDown="InventoryReportCard_Click"
                       Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Icon -->
                        <Grid Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <Ellipse Style="{StaticResource IconCircleStyle}">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#50C878" Offset="0"/>
                                        <GradientStop Color="#3FA760" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="📦" 
                                      FontSize="36" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Content -->
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="Inventory Report" 
                                      FontSize="24" 
                                      FontWeight="Bold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="Monitor stock levels, track inventory movements, identify low stock items, and analyze stock valuation across all products."
                                      FontSize="13"
                                      Foreground="{DynamicResource TextSecondary}"
                                      TextWrapping="Wrap"
                                      LineHeight="20"/>
                        </StackPanel>

                        <!-- Action Button -->
                        <Button Grid.Row="2" 
                               Content="View Report →"
                               HorizontalAlignment="Left"
                               Background="Transparent"
                               BorderThickness="0"
                               Foreground="#50C878"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Padding="0,10,0,0"
                               Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#3FA760"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Cash Report Card -->
                <Border Grid.Row="1" Grid.Column="0" 
                       Style="{StaticResource ReportCardStyle}"
                       Opacity="0.6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Icon -->
                        <Grid Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <Ellipse Style="{StaticResource IconCircleStyle}">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FFB84D" Offset="0"/>
                                        <GradientStop Color="#F5A623" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="💰" 
                                      FontSize="36" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Content -->
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="Cash Report" 
                                      FontSize="24" 
                                      FontWeight="Bold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="Track cash flow, analyze payment methods, review shift summaries, and monitor cash drawer reconciliation."
                                      FontSize="13"
                                      Foreground="{DynamicResource TextSecondary}"
                                      TextWrapping="Wrap"
                                      LineHeight="20"/>
                        </StackPanel>

                        <!-- Coming Soon Badge -->
                        <Border Grid.Row="2"
                               Background="#FFF3CD"
                               CornerRadius="12"
                               Padding="10,5"
                               HorizontalAlignment="Left"
                               Margin="0,10,0,0">
                            <TextBlock Text="Coming Soon"
                                      FontSize="12"
                                      FontWeight="SemiBold"
                                      Foreground="#856404"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Customer Report Card -->
                <Border Grid.Row="1" Grid.Column="2" 
                       Style="{StaticResource ReportCardStyle}"
                       MouseLeftButtonDown="CustomerReportCard_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Icon -->
                        <Grid Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,20">
                            <Ellipse Style="{StaticResource IconCircleStyle}">
                                <Ellipse.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#E74C3C" Offset="0"/>
                                        <GradientStop Color="#C0392B" Offset="1"/>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="👥" 
                                      FontSize="36" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Content -->
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="Customer Report" 
                                      FontSize="24" 
                                      FontWeight="Bold"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,0,0,10"/>
                            <TextBlock Text="Analyze customer purchase patterns, identify top customers, track loyalty metrics, and review customer lifetime value."
                                      FontSize="13"
                                      Foreground="{DynamicResource TextSecondary}"
                                      TextWrapping="Wrap"
                                      LineHeight="20"/>
                        </StackPanel>

                        <!-- Action Button -->
                        <Button Grid.Row="2" 
                               Content="View Report →"
                               HorizontalAlignment="Left"
                               Background="Transparent"
                               BorderThickness="0"
                               Foreground="#E74C3C"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Padding="0,10,0,0"
                               Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#C0392B"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
