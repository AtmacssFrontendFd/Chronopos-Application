<UserControl x:Class="ChronoPos.Desktop.Views.PriceTypesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ChronoPos.Desktop.Views.Controls"
             xmlns:converters="clr-namespace:ChronoPos.Desktop.Converters"
             xmlns:userControls="clr-namespace:ChronoPos.Desktop.UserControls"
             mc:Ignorable="d" 
             Background="{DynamicResource MainBackground}"
             FlowDirection="{Binding CurrentFlowDirection}"
             FontFamily="{DynamicResource PoppinsFontFallback}">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:FlowDirectionToGridColumnConverter x:Key="FlowDirectionToGridColumnConverter"/>
        <converters:FlowDirectionToHorizontalAlignmentConverter x:Key="FlowDirectionToHorizontalAlignmentConverter"/>
        <converters:BoolToGridLengthConverter x:Key="BoolToGridLengthConverter"/>

        <!-- Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource BorderCornerRadius}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDarker}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource BorderLight}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Action Button Style -->
        <Style x:Key="SmallActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeExtraSmall}"/>
            <Setter Property="FontFamily" Value="{DynamicResource PoppinsFontFallback}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="PriceTypeCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource BorderCornerRadius}"/>
            <Setter Property="Margin" Value="{DynamicResource DefaultMarginThickness}"/>
            <Setter Property="Padding" Value="{DynamicResource DefaultPaddingThickness}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="{DynamicResource DefaultMarginThickness}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Back Button and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Back Button -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentFlowDirection}" Value="RightToLeft">
                                    <Setter Property="FlowDirection" Value="LeftToRight"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <controls:CircularBackButton 
                        Command="{Binding GoBackCommand}"
                        Width="{DynamicResource ButtonHeight}"
                        Height="{DynamicResource ButtonHeight}"
                        Margin="0,0,10,0"/>
                </StackPanel>

                <!-- Title -->
                <TextBlock Text="Price Types" 
                           FontSize="{DynamicResource FontSizeLarge}"
                           FontWeight="Bold"
                           FontFamily="{DynamicResource PoppinsFontFallback}"
                           Foreground="{DynamicResource TextPrimary}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <!-- Refresh Button -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentFlowDirection}" Value="RightToLeft">
                                    <Setter Property="FlowDirection" Value="LeftToRight"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <controls:RefreshButton 
                        Command="{Binding LoadPriceTypesCommand}"
                        ButtonText="Refresh"
                        ButtonIcon="🔄"
                        MinButtonWidth="120"
                        ButtonHeight="36"
                        Margin="0,0,10,0"/>
                </StackPanel>

                <!-- Add Price Type Button -->
                <Button Content="Add Price Type"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding AddPriceTypeCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Search and Filter Section -->
        <Border Grid.Row="1" Style="{StaticResource PriceTypeCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="{DynamicResource FontSizeSmall}"
                         FontFamily="{DynamicResource PoppinsFontFallback}"
                         Padding="{DynamicResource DefaultPaddingThickness}"
                         Margin="0,0,10,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderLight}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{DynamicResource BorderCornerRadius}">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                            Margin="{TemplateBinding Padding}"
                                                            VerticalAlignment="Center"/>
                                                <TextBlock Text="Search price types..."
                                                         Margin="{TemplateBinding Padding}"
                                                         VerticalAlignment="Center"
                                                         Foreground="{DynamicResource TextSecondary}"
                                                         FontFamily="{TemplateBinding FontFamily}"
                                                         FontSize="{TemplateBinding FontSize}"
                                                         IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Filter Buttons -->
                <Button Grid.Column="1" Content="{Binding ActiveFilterButtonText}" 
                        Style="{StaticResource SmallActionButtonStyle}"
                        Command="{Binding ToggleActiveFilterCommand}"
                        Margin="0,0,10,0"/>

                <Button Grid.Column="2" Content="Clear Search" 
                        Style="{StaticResource SmallActionButtonStyle}"
                        Command="{Binding ClearSearchCommand}"/>
            </Grid>
        </Border>

        <!-- Price Types Table -->
        <Border Grid.Row="2" Style="{StaticResource PriceTypeCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Table Header -->
                <TextBlock Grid.Row="0" 
                           Text="Price Types List"
                           FontSize="{DynamicResource FontSizeMedium}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           Margin="0,0,0,10"/>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding FilteredPriceTypes}"
                          SelectedItem="{Binding SelectedPriceType}"
                          AutoGenerateColumns="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False"
                          SelectionMode="Single"
                          Background="{DynamicResource SurfaceBackground}"
                          Foreground="{DynamicResource TextPrimary}"
                          BorderBrush="{DynamicResource BorderLight}"
                          HorizontalGridLinesBrush="{DynamicResource BorderLight}"
                          VerticalGridLinesBrush="{DynamicResource BorderLight}"
                          AlternatingRowBackground="{DynamicResource AlternatingRowBackground}"
                          FontFamily="{DynamicResource PoppinsFontFallback}"
                          FontSize="{DynamicResource FontSizeSmall}">

                    <DataGrid.Columns>
                        <!-- Type Name -->
                        <DataGridTextColumn Header="Type Name" 
                                            Binding="{Binding TypeName}" 
                                            Width="200"
                                            IsReadOnly="True"/>

                        <!-- Arabic Name -->
                        <DataGridTextColumn Header="Arabic Name" 
                                            Binding="{Binding ArabicName}" 
                                            Width="200"
                                            IsReadOnly="True"/>

                        <!-- Description -->
                        <DataGridTextColumn Header="Description" 
                                            Binding="{Binding Description}" 
                                            Width="300"
                                            IsReadOnly="True"/>

                        <!-- Status -->
                        <DataGridTextColumn Header="Status" 
                                            Binding="{Binding Status, Converter={x:Static converters:BooleanToStringConverter.Instance}, ConverterParameter='Active;Inactive'}" 
                                            Width="100"
                                            IsReadOnly="True"/>

                        <!-- Actions -->
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Edit Button -->
                                        <Button Content="✏️" 
                                                ToolTip="Edit"
                                                Style="{StaticResource SmallActionButtonStyle}"
                                                Command="{Binding DataContext.EditPriceTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="2"/>

                                        <!-- Delete Button -->
                                        <Button Content="🗑️" 
                                                ToolTip="Delete"
                                                Style="{StaticResource SmallActionButtonStyle}"
                                                Command="{Binding DataContext.DeletePriceTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Status Bar -->
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding StatusMessage}"
                               Foreground="{DynamicResource TextSecondary}"
                               FontSize="{DynamicResource FontSizeExtraSmall}"
                               VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1"
                               Text="{Binding PriceTypes.Count, StringFormat='Total: {0} price types'}"
                               Foreground="{DynamicResource TextSecondary}"
                               FontSize="{DynamicResource FontSizeExtraSmall}"
                               VerticalAlignment="Center"/>
                </Grid>
            </Grid>
        </Border>
        </Grid>

        <!-- Side Panel for Add/Edit -->
        <Grid Grid.Column="1" 
              Width="{Binding IsSidePanelOpen, Converter={StaticResource BoolToGridLengthConverter}, ConverterParameter='450'}">
            <Border Background="{DynamicResource SidePanelBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0"
                    Visibility="{Binding IsSidePanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Panel Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="{Binding IsEditMode, Converter={x:Static converters:BooleanToStringConverter.Instance}, ConverterParameter='Edit Price Type;Add Price Type'}"
                                   FontSize="{DynamicResource FontSizeMedium}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   VerticalAlignment="Center"/>

                        <Button Grid.Column="1"
                                Content="✕"
                                Command="{Binding CancelEditCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{DynamicResource TextSecondary}"
                                FontSize="{DynamicResource FontSizeMedium}"
                                Padding="8"
                                Cursor="Hand"/>
                    </Grid>

                    <!-- Form Fields -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,10,0,0">
                            
                            <!-- Type Name -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="Type Name*"
                                           Foreground="{DynamicResource TextPrimary}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8" />
                                <TextBox Text="{Binding CurrentPriceType.TypeName, UpdateSourceTrigger=PropertyChanged}"
                                         Background="{DynamicResource InputBackground}"
                                         Foreground="{DynamicResource TextPrimary}"
                                         BorderBrush="{DynamicResource BorderBrush}"
                                         BorderThickness="1"
                                         Padding="12,10"
                                         FontSize="{DynamicResource FontSizeDefault}"
                                         MinHeight="40" />
                            </StackPanel>

                            <!-- Arabic Name -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="Arabic Name*"
                                           Foreground="{DynamicResource TextPrimary}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8" />
                                <TextBox Text="{Binding CurrentPriceType.ArabicName, UpdateSourceTrigger=PropertyChanged}"
                                         Background="{DynamicResource InputBackground}"
                                         Foreground="{DynamicResource TextPrimary}"
                                         BorderBrush="{DynamicResource BorderBrush}"
                                         BorderThickness="1"
                                         Padding="12,10"
                                         FontSize="{DynamicResource FontSizeDefault}"
                                         FlowDirection="RightToLeft"
                                         MinHeight="40" />
                            </StackPanel>

                            <!-- Description -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="Description"
                                           Foreground="{DynamicResource TextPrimary}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8" />
                                <TextBox Text="{Binding CurrentPriceType.Description, UpdateSourceTrigger=PropertyChanged}"
                                         Background="{DynamicResource InputBackground}"
                                         Foreground="{DynamicResource TextPrimary}"
                                         BorderBrush="{DynamicResource BorderBrush}"
                                         BorderThickness="1"
                                         Padding="12,10"
                                         FontSize="{DynamicResource FontSizeDefault}"
                                         Height="100"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         VerticalScrollBarVisibility="Auto" />
                            </StackPanel>

                            <!-- Status -->
                            <StackPanel Margin="0,0,0,20">
                                <CheckBox Content="Active"
                                          IsChecked="{Binding CurrentPriceType.Status}"
                                          Foreground="{DynamicResource TextPrimary}"
                                          FontWeight="SemiBold"
                                          FontSize="{DynamicResource FontSizeDefault}" />
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                        <Button Content="Cancel"
                                Command="{Binding CancelEditCommand}"
                                Background="{DynamicResource SurfaceBackground}"
                                Foreground="{DynamicResource TextPrimary}"
                                Padding="20,12"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource BorderBrush}"
                                FontWeight="SemiBold"
                                MinWidth="100"
                                Margin="0,0,15,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource OverlayBackground}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Save"
                                Command="{Binding SavePriceTypeCommand}"
                                Style="{StaticResource ActionButtonStyle}"
                                Padding="20,12"
                                FontWeight="SemiBold"
                                MinWidth="100"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>